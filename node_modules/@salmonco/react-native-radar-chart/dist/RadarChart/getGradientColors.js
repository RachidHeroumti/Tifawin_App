import Color from 'color';
export var getGradientColors = function (startColor, endColor, count) {
    var start = Color(startColor);
    var end = Color(endColor);
    var gradientColors = [];
    for (var i = 0; i < count; i++) {
        var interpolation = i / (count - 1);
        var color = start.mix(end, interpolation);
        gradientColors.push(color.hex());
    }
    return gradientColors;
};
//# sourceMappingURL=getGradientColors.js.map